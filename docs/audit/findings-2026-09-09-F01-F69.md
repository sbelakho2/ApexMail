F01 · P1 — Production and important database tests use different migrations.
Files: api-server/src/lib.rs:32, also line 264; api-server/src/routes/messages.rs:1501; routes/scim.rs:1520; routes/admin/support.rs:635; routes/admin/warmup.rs:239; integration-tests/tests/schema_contract_tests.rs:28; sales-autopilot/tests/common/mod.rs:154,210.

Tests bootstrap archived tools/migrations, while deployment uses services/mail-server/migrations. Their identifier and column shapes differ.

Fix: Centralize test database creation around the actual production migrator and all canonical migrations. Invoke the same runtime initializers as the corresponding production service. Keep archive compatibility tests explicitly separate. Replace the listed legacy bootstrap helpers.

Verify: Run populated signup, sending, invoice, contact, SCIM, webhook, and console tests on a freshly migrated PostgreSQL 16 database.

F02 · P1 — Several console pages decode UUIDs as strings.
File: api-server/src/routes/web/data.rs:573, also 672, 822, 1092, 1723, 2345, 2477.

Contacts, lists, domains, campaigns, alerts, and control-plane domains select UUID id columns into String tuples. Empty databases conceal the decoding failure.

Fix: Select id::text AS id, or decode uuid::Uuid and convert at the presentation boundary. Apply this consistently to every listed loader. campaigns.sent_count is INTEGER, so its existing Option<i32> should remain.

Verify: Seed actual canonical rows and assert that each page displays their values and correct detail links without ColumnDecode errors.

F58 · P1 — IP pool rows also have incompatible UUID and INET decoding.
File: api-server/src/routes/web/data.rs:2227, also 2230.

ip_pool_addresses.id is UUID and ip_address is INET; the loader expects strings.

Fix: Select id::text AS id and host(ip_address) AS ip_address. Use ip_address::text instead only if displaying its network prefix is intentional. Alternatively, use the corresponding typed Rust values.

Verify: Populate IPv4 and IPv6 addresses and check displayed addresses, statuses, warmup days, and action targets.

F03 · P1 — The API Keys console selects a nonexistent column.
File: api-server/src/routes/web/data.rs:1310.

The schema has key_prefix, not prefix; the selected UUID also needs correct decoding.

Fix: Use:

SELECT id::text AS id, name, key_prefix AS prefix,
       created_at, revoked_at, expires_at
FROM api_keys
WHERE tenant_id = $1
ORDER BY created_at DESC
LIMIT 100;

Update the view model to distinguish active, expired, and revoked keys.

Verify: Create all three states through the API and confirm accurate display without exposing secret hashes or complete keys.

F04 · P1 — The billing console hides invoices and calculates incorrect outstanding totals.
File: api-server/src/routes/web/data.rs:1461, also 1439, 1471.

It selects absent amount_cents, decodes UUID as String, treats every non-paid invoice as debt, and labels totals EUR.

Fix: Read canonical total, currency, status, and id::text. Calculate collectible outstanding amounts from payment allocations, excluding draft/void balances. Aggregate across the full eligible dataset, independently of the visible 50-row limit. Display separate currency totals.

Verify: Include partial payments, multiple currencies, draft/void invoices, and more than 50 invoices.

F05 · P1 — Invoice list/detail fail on both their primary and fallback queries.
File: api-server/src/routes/billing.rs:2093, also 2132, 2178, 2215.

The canonical query selects absent xml_url; the fallback selects absent legacy fields including amount_cents.

Fix: Add and populate nullable xml_url TEXT if XML export exists. Otherwise select NULL::text AS xml_url. Keep canonical totals and due_at. Select schema adapters explicitly; do not treat arbitrary missing-column errors as permission to execute an incompatible fallback.

Verify: List and retrieve real invoices with null and populated optional export URLs.

F06 · P1 — Plan override creation/update references absent updated_at.
File: api-server/src/routes/billing.rs:3729.

The conflict branch also preserves active=false, and malformed expiry can become an unlimited override.

Fix: Add updated_at TIMESTAMPTZ NOT NULL DEFAULT now() in a new canonical migration. Explicitly set active=true, validated plan/reason/admin/expiry, and updated_at=now() on replacement. Reject malformed expiry. Commit the override and audit record together, then invalidate entitlement caches.

Verify: Create, deactivate, and reissue an override; inject audit-write failure and check atomic rollback.

F07 · P1 — Bulk contact tagging writes nonexistent contacts.tags.
File: api-server/src/routes/contacts.rs:687.

Fix: Establish one representation across contact create/read/update/export. A concrete option is tags JSONB NOT NULL DEFAULT '[]'::jsonb with an array-shape constraint, validated bounded strings, and set-union updates. A normalized tenant-scoped relation is also valid, but every consumer must use it.

Verify: Repeated and overlapping tagging operations produce unique tags, preserve tenant isolation, and appear in detail/list/export responses.

F08 · P1 — Billing address consumers require nonexistent fields.
Files: billing-service/src/accounting_export.rs:75; api-server/src/routes/billing.rs:4014.

Consumers select state and/or email, absent from canonical billing_addresses.

Fix: Add nullable fields and update authoritative address readers/writers with validation. Issued invoices and exports must use an immutable billing-address snapshot, not the account’s subsequently edited address.

Verify: Invoice/export addresses with and without those fields; changing the account address must not alter historical invoices.

F09 · P1 — Payment recovery references nonexistent abuse-case status.
Files: billing-service/src/maintenance.rs:3215; api-server/src/routes/billing.rs:3861.

Both queries use abuse_reports.status, which does not exist.

Fix: Implement the abuse lifecycle with constrained statuses, a conservative historical backfill, and creation/review/resolution writers. Keep billing and abuse holds distinct. Recovery must clear only the relevant billing hold and release sending only when every applicable hold permits it.

Verify: Payment restores a billing-only restriction while an unresolved abuse hold remains effective.

F10 · P1 — Webhook retry branches write the wrong error column.
File: worker-processors/src/webhook/processor.rs:373, also 768, 828.

webhook_queue has error_message, not last_error.

Fix: Replace the column in all three updates. Preserve attempt counting, scheduling, and lease release. If completion can outlive ownership, fence updates with the claim’s owner token.

Verify: Exercise circuit-open, 429/503, and retry-budget failure branches; each must persist the intended retry state.

F11 · P1 — AI draft approval compares a string ID against UUID.
File: api-server/src/routes/admin/ai_drafts.rs:129, also 153.

Canonical inbound_messages.id is character varying; $1::uuid makes the comparison invalid.

Fix: Bind the canonical string identifier without a UUID cast in approval and restoration paths. Preserve conditional claim semantics. Persist a reply/outbox operation before consuming approval.

Verify: Concurrent approvals enqueue exactly one reply, and an enqueue failure leaves a recoverable draft.

F12 · P2 — Bot detection silently loses its send-time signal.
File: api-server/src/routes/ai_insights.rs:427.

The query compares UUID messages.id with varchar events.message_id; .ok().flatten() hides the error.

Fix: Use a valid identifier boundary such as messages.id::text = events.message_id, with tenant predicates on both sides. Handle nullable sent_at correctly and distinguish query failure from missing evidence.

Verify: A genuine very-fast open triggers the intended signal; unrelated tenants and absent timestamps do not.

F13 · P2 — Unsubscribe attribution queries nonexistent messages.to_address.
File: tracking-service/src/routes/unsubscribe.rs:585.

It also decodes UUID as String, then substitutes an invented message ID after failure.

Fix: Include the originating message ID in new signed unsubscribe tokens. Resolve legacy tokens against canonical recipient arrays with tenant scope and id::text. Represent unresolved attribution as unknown.

Verify: Known messages retain their actual attribution; unresolved attribution never prevents suppression persistence.

F14 · P1 — Database failures become successful empty console states.
File: api-server/src/routes/web/data.rs:113, also 122, 139.

optional_rows converts errors to empty lists; count_rows converts them to zero.

Fix: Return typed loaded/empty/unavailable states. Required financial and operational data must preserve uncertainty instead of reporting false zeroes. Log structured query identity and a correlation ID without sensitive parameters.

Verify: Database outage, missing-column, and decoding failures produce an unavailable state rather than “no invoices,” “no keys,” or zero backlog.

Next, make identity and authorization consistent across all entry points.

F15 · P1 — Email verification fails against canonical UUID user IDs.
File: api-server/src/routes/auth.rs:2998, also 3033.

The lookup returns text, then the update binds that text against UUID users.id. The database reproduction produced 42883.

Fix: Return and bind uuid::Uuid, or parse a validated UUID before binding. Implement F16 in the same change.

Verify: Complete real signup → token verification → login on the canonical schema.

F16 · P1 — Verification expiry and consumption are not enforced atomically.
File: api-server/src/routes/auth.rs:3018, also 3033, 3043.

Missing/malformed expiry is accepted. Token lookup and mutations are separate. Tenant activation can overwrite an independent suspension.

Fix: Require a valid future expiry. Consume the matching token using conditional update/locking in one transaction. Store expiry as a typed timestamp. Activate only the initial pending-verification state; preserve administrative, abuse, and billing holds.

Verify: Exactly one concurrent token use succeeds; expired/malformed tokens fail; injected failures leave no partial verification.

F17 · P1 — Wildcard administrators cannot create ordinary scoped keys.
File: api-server/src/routes/auth.rs:3084.

The subset check requires literal equality, so ["*"] does not authorize requesting messages:send.

Fix: Validate requested scopes against the shared registry, then authorize each when the caller has * or that scope. A restricted caller must not mint *.

Verify: Owners can issue ordinary keys; restricted users cannot escalate or invent scope names.

F18 · P1 — Tenant suspension does not itself block API authentication or queued delivery.
Files: api-server/src/middleware/auth.rs:345, also 943; routes/admin/tenants.rs:334; worker-processors/src/email/processor.rs:180.

These paths check credential/user/domain state without consistently enforcing tenants.status.

Fix: Centralize tenant policy for API, SSR, SMTP credentials, and dispatch. Check before accepting work and before external sending. Invalidate/version cached decisions and permit only explicitly defined recovery operations during restrictions.

Verify: Suspend an already authenticated tenant with valid keys and queued mail; restricted operations and deliveries must stop across replicas.

F46 · P2 — Console-created API keys bypass the JSON endpoint’s expiry policy.
Files: api-server/src/routes/web.rs:3165; routes/auth.rs:3122.

SSR omits expires_at and hardcodes scopes.

Fix: Share key creation logic across SSR/JSON: scope authorization, expiry defaults/bounds, hashing, atomic key-count enforcement, and persistence. Expose supported choices in the form while retaining one-time secret display and CSRF protection.

Verify: Equivalent requests produce equivalent credentials; concurrent creation cannot exceed the tenant limit.

F64 · P2 — Overage estimates accept another tenant’s identifier.
File: api-server/src/routes/billing.rs:2763, also 2778; app.rs:628.

The handler lacks an AuthUser ownership check but reads entitlement information using the payload’s tenant ID.

Fix: Use the authenticated tenant for normal estimates. Require a separate explicit administrative authorization for another tenant. Public calculators should take public plans and hypothetical usage without tenant lookup.

Verify: Tenant A cannot retrieve B’s allowance; A’s estimate matches its actual effective pricing.

F65 · P2 — Verification tokens moved into paths still enter access logs.
Files: deploy/nginx/nginx.conf:17; api-server/src/routes/auth.rs:2976; app.rs:725.

Nginx logs the full $request and referrer, including token-bearing URLs.

Fix: Redact at the first logging boundary. Log normalized routes and correlation IDs. Configure application spans similarly, apply an appropriate referrer policy, and redirect successful token exchange to a clean URL.

Verify: A recognizable test token never appears in proxy/application logs or follow-up referrers, including error paths.

Then repair request acceptance and delivery as one connected lifecycle.

F44 · P1 — Caller metadata can poison a shared queue claim.
Files: api-server/src/routes/messages.rs:81, also 594, 865; worker-processors/src/email/processor.rs:277.

A JSON number/string is accepted as metadata. The worker’s jsonb_set fails on that scalar. The actual claim reproduction failed with 22023, leaving healthy sibling jobs pending too.

Fix: Require bounded object-shaped customer metadata. Separate it from operational queue state. Constrain the stored operational shape and repair/quarantine historical invalid rows so healthy work can proceed.

Verify: Test numbers, strings, arrays, null, omitted metadata, and objects. Null may normalize to missing. Historical poison rows must not repeatedly block healthy batches.

F45 · P1 — Customer metadata can override validated recipients.
Files: worker-processors/src/email/processor.rs:310, also 681; api-server/src/routes/messages.rs:487,1202,1355.

The worker trusts customer-controlled metadata.pending_recipients. The reproduction returned a different delivery address from the validated envelope. Empty arrays can suppress delivery; larger arrays can undermine recipient accounting.

Fix: Make pending recipients, leases, and attempts exclusively server-written fields. Derive recipients from validated envelope records and enforce a subset invariant on retries. Reconstruct existing queue state from trusted delivery records.

Verify: Customer metadata containing alternate recipients, empty arrays, or fake operational fields cannot change delivery or quota.

F19 · P1 — Idempotency is not bound to the request being replayed.
File: api-server/src/middleware/idempotency.rs:58, also 76; app.rs:672; routes/messages.rs:567.

Cached responses are not bound to method, route, and payload; safe reads are not excluded. The single-send database fallback also does not compare payloads.

Fix: Define tenant/principal/method/route/key identity and persist a canonical payload hash. Return 409 for conflicting reuse. Bypass safe reads and recheck current operation authorization before replay.

Verify: Same-request replay works; changed body, route, method, or permissions cannot produce an unrelated cached success.

F20 · P1 — Batch idempotency disappears after response loss or Redis failure.
Files: api-server/src/routes/messages.rs:755, also 858; middleware/idempotency.rs:99,260,282.

Batches lack a durable operation/item identity. Redis failure proceeds to execution, and client disappearance can prevent response caching.

Fix: Commit a durable batch ledger, payload hash, stable item IDs, results, messages, and queue/outbox records together. Persist results independently of response consumption. Redis should accelerate this ledger.

Verify: Commit, disconnect before receiving the response, restart, disable Redis, and retry: identical IDs/results return without another send or charge.

F21 · P1 — Idempotency leases have no ownership fencing.
File: api-server/src/middleware/idempotency.rs:174, also 21, 313, 328.

A shared 30-second marker expires; completion and deletion are unconditional. An old owner can overwrite a newer owner.

Fix: Use random owner tokens, renewal, and compare-owner completion/release operations. Keep the durable request ledger authoritative.

Verify: A expires, B claims, then A completes/releases: B’s lease/result remains unchanged.

F22 · P1 — Concurrent duplicate sends reserve quota twice.
File: api-server/src/routes/messages.rs:633, also 656, 726.

Reservation precedes insertion. A uniqueness loser returns the existing message without compensating its reservation.

Fix: Return explicit inserted/duplicate outcomes. Compensate only the losing operation, idempotently. Prefer a stable logical usage ID that makes double reservation impossible.

Verify: Synchronize duplicate requests before insertion with caching disabled; one message, one recipient set, and one quota debit must remain.

F23 · P1 — Batch responses can report accepted messages that were rolled back.
File: api-server/src/routes/messages.rs:781, also 861, 904, 930.

Items share a transaction; SQL errors are caught without savepoints. The database reproduction showed COMMIT completing as ROLLBACK, with earlier rows absent.

Fix: For partial success, wrap each item’s complete mutation in a savepoint and compensate failed-item quota. Alternatively make the batch explicitly atomic and report no accepted items after failure. Validate forbidden text values before reservation.

Verify: Valid A, SQL-failing B, valid C must produce exactly the documented durable result and corresponding quota.

F24 · P1 — Cancellation races worker claims.
Files: api-server/src/routes/messages.rs:501, also 522, 536; worker-processors/src/email/processor.rs:277.

Cancellation locks the parent but checks unlocked children; workers claim those children independently.

Fix: Establish a shared lock order and cancellation protocol. Lock eligible queue rows before deciding, verify affected counts, and atomically transition parent/children. Define the irreversible dispatch boundary and reject cancellation after it.

Verify: Force a claim between eligibility checking and cancellation. Outcomes must be “cancelled with no delivery” or an explicit too-late result.

F25 · P1 — Scheduled and multi-recipient parent statuses are wrong.
Files: worker-processors/src/email/processor.rs:160, also 1934; api-server/src/routes/messages.rs:227.

The worker accepts only queued for its parent transition, leaving scheduled messages behind. It does not set sent_at; the first successful recipient can mark the whole message sent.

Fix: Persist recipient-level state and derive parent progress from the complete set. Define scheduled → processing, partial success, accepted, delivered, and terminal outcomes, including timestamp semantics.

Verify: Scheduled SMTP and mixed success/retry/bounce recipients produce truthful parent state and timestamps.

F26 · P1 — Per-recipient expansion loses original To/Cc headers.
Files: api-server/src/routes/messages.rs:429, also 465; worker-processors/src/email/processor.rs:681; email/transport.rs:373,641,746.

Queue rows retain individual envelope recipients but lose the original visible addressing.

Fix: Store MIME To/Cc separately from envelope destinations. Preserve visible addresses and logical Message-ID across copies. Keep Bcc exclusively in delivery data.

Verify: Capture MIME for two To recipients, one Cc, and one Bcc: identical intended To/Cc, no Bcc header, and exactly the authorized envelope deliveries.

Billing needs an immutable period model and a payment allocation ledger before its retry and reporting paths can be dependable.

F27 · P1 — Stripe subscription upsert omits the stored plan and preserves stale prices.
File: billing-service/src/stripe_webhooks.rs:894, also 904; overage.rs:150,1010.

Paid checks use stripe_subscriptions.plan, but the main upsert never writes it. Conflict updates also omit the changed price ID.

Fix: Persist the resolved plan and Stripe price on insert/update. Backfill null plans from authoritative price mappings; flag unresolved rows. Keep historical period snapshots separate from temporary entitlement status.

Verify: Creation, upgrade, downgrade, and termination preserve correct current entitlement and historical pricing.

F28 · P1 — Three existence checks decode INT4 into i64.
File: billing-service/src/overage.rs:574, also 612, 1001; caller usage.rs:751.

SELECT 1 returns INT4; the helpers expect Option<i64>. Matching rows expose the mismatch.

Fix: Use SELECT EXISTS(...) decoded as bool, or explicitly select 1::bigint. Fix the absent subscription plan simultaneously because it can mask this failure.

Verify: Each helper returns correctly for zero and one matching row through actual SQLx decoding.

F29 · P1 — The overage lock ends before invoice insertion.
File: billing-service/src/overage.rs:595, also 626, 338; migration 126_overage_period_marker.sql:35.

The advisory-lock transaction commits before invoice creation. The period index is not unique.

Fix: Claim the period and insert its invoice in one transaction. Enforce uniqueness on the actual billing identity—tenant, usage kind, and period. Reconcile existing duplicates first. Conflicts must return the existing invoice without collecting again.

Verify: Two synchronized sweep instances create exactly one invoice and collection operation.

F30 · P1 — Renewal can erase the period before overage billing reads it.
Files: billing-service/src/overage.rs:150; stripe_webhooks.rs:904; maintenance.rs:248.

Renewal overwrites mutable cycle dates before the daily sweep discovers the ended cycle.

Fix: Add immutable billing-period records containing tenant/subscription, start/end, currency, price/allowance snapshot, metering watermark, and invoice state. Close periods transactionally and enqueue idempotent billing work. Reconcile missed historical periods before their metering expires.

Verify: Renewal before/after the sweep, duplicate/out-of-order webhooks, and downtime all produce the same single invoice.

F31 · P1 — LIMIT 500 can repeatedly revisit completed periods and starve later ones.
File: billing-service/src/overage.rs:150, also 158.

Fix: Select unresolved period records, exclude completed work in SQL, and use stable keyset pagination or durable SKIP LOCKED claims. Continue bounded batches and monitor oldest unresolved period age.

Verify: Seed 1,201 periods with completed and zero-overage entries among the oldest 500. Every eligible period must progress without aging out of the window.

F32 · P1 — Override allowance is combined with another plan’s rate.
File: billing-service/src/overage.rs:185, also 211.

The allowance uses COALESCE(po.plan,t.plan) but later pricing uses t.plan.

Fix: Immediately select the same effective plan for both. Ultimately price from the immutable period’s complete allowance/rate/currency snapshot, including explicit overrides. Use checked integer arithmetic.

Verify: Different base/override rates yield the override’s correct invoice; later plan changes cannot rewrite past pricing.

F33 · P1 — Collection can strand draft invoices and ignores commit failures.
File: billing-service/src/overage.rs:676, also 686, 778, 833.

Invoice creation commits first. Collection failure can leave a draft that later sweeps skip. Commit results are discarded, and the claimed dunning handoff lacks durable work.

Fix: Create a collection outbox operation with the invoice. Implement resumable issued/partially-paid/paid/past-due transitions, check every commit, and explicitly enqueue local/PAYG dunning. Keep external calls outside wallet-lock transactions.

Verify: Crash/fail every step; restart must settle once or expose a genuinely retryable outstanding operation.

F34 · P1 — Stripe invoice-item IDs are stored as invoice IDs.
Files: billing-service/src/overage.rs:787, also 847; stripe_webhooks.rs:1225.

An ii_ identifier is stored in stripe_invoice_id; subsequent invoice.paid events identify an in_ invoice.

Fix: Store item/invoice identifiers separately with an explicit mapping to the original local invoice. Create/finalize the intended external invoice and reconcile its lines/payments idempotently. Final usage must remain collectible without a future subscription renewal.

Verify: Partial wallet plus Stripe settlement pays the original invoice exactly once, without creating a second logical invoice.

F35 · P1 — Wallet deductions lack durable invoice payment allocations.
File: billing-service/src/overage.rs:723, also 755, 840.

Partial wallet payment is not authoritatively reflected in invoice amount due.

Fix: Add immutable payment allocations with unique operation IDs. Commit wallet movement and its invoice allocation together. Derive outstanding balance from invoice total, confirmed payments, and valid credits. Make UI, dunning, refunds, and credit limits use it.

Verify: A 100-unit invoice with 40 wallet units exposes 60 due and collects exactly 60 externally under retries/crashes.

F36 · P1 — Deleting an old subscription can downgrade a newer active subscription.
File: billing-service/src/stripe_webhooks.rs:879, also 1139.

An old subscription’s deletion unconditionally sets the tenant to free.

Fix: Reconcile entitlement under a tenant-scoped transaction using the current subscription and applicable active/trial/grace state. Reject stale event effects using ordering or authoritative Stripe reconciliation.

Verify: Create B after A, then deliver A’s deletion/update events. B’s valid entitlement must survive.

F66 · P2 — Hourly subscription cleanup targets a nonexistent persistence model.
File: billing-service/src/maintenance.rs:323, also 3320, 3330.

The hourly task updates absent subscription_change_saga. No corresponding producer or initializer was found. This demonstrates a failing cleanup job, not a proven loss of live saga records.

Fix: Connect subscription-change persistence and recovery: canonical operation records, producer, constrained transitions, external operation IDs, and restart reconciliation. Reconcile Stripe state before deciding that a timed-out operation failed.

Verify: Crash after external success; recovery converges exactly once without 42P01.

Recipient consent must be persisted correctly and enforced immediately before dispatch.

F54 · P1 — Suppression/preference IDs exceed their column lengths.
Files: tracking-service/src/processor.rs:867, also 920, 1022, 1219; routes/unsubscribe.rs:370,481,670.

sup_/prf_ plus 32 hex characters produces 36-character IDs for VARCHAR(26) columns. Database inserts reproduced 22001.

Fix: Use the platform’s 26-character entity ID generator in immediate and retry paths. Do not blindly shorten IDs for unrelated tables. Repair and replay pending suppression operations.

Verify: One-click unsubscribe, retry, unsubscribe-all, and category preferences persist successfully on canonical migrations.

F55 · P1 — A new unsubscribe does not invalidate cached permission to send.
Files: worker-processors/src/email/processor.rs:1252, also 35, 1316; tracking-service/src/processor.rs:939.

Negative suppression results live for five minutes. Publishing suppression:added does not invalidate these worker caches; no subscriber was found.

Fix: Check authoritative current consent before sending, or implement versioned consent with reliable invalidation and missed-event recovery. Cover category changes and resubscriptions, with consistent recipient normalization.

Verify: Prime the cache, unsubscribe on another replica, and dispatch immediately—including after a missed notification. No prohibited send may occur.

F37 · P1 — Malformed Unicode unsubscribe tokens can panic before verification.
File: tracking-service/src/routes/unsubscribe.rs:37, also 56.

Logging slices the string at byte 20, which can lie inside a Unicode character.

Fix: Validate the token’s ASCII alphabet/structure before processing and remove raw token material from logs. Any legitimate text prefixing must use character boundaries.

Verify: Multi-byte and mixed tokens around the boundary return controlled 4xx responses without panics or token leakage.

F38 · P1 — Unsubscribe → resubscribe → unsubscribe can lose the final choice.
File: tracking-service/src/routes/unsubscribe.rs:101, also 393, 555.

A 24-hour tenant/recipient dedup key survives resubscription, so the next unsubscribe returns success without restoring suppression.

Fix: Always idempotently persist the requested current consent state. Deduplicate events separately or use consent-version identity. Keep state/version changes durable and invalidate sending caches.

Verify: Perform the three transitions within a minute, with duplicate requests and Redis loss; the final unsubscribe must govern sending.

F39 · P2 — Manual unsubscribe mutates state through GET.
Files: tracking-service/src/routes/unsubscribe.rs:175; templates.rs:115.

GET ?confirm=1 changes consent, making navigation/prefetch capable of triggering it.

Fix: Render a POST confirmation form and keep GET side-effect free. Preserve the separate one-click POST contract and accessible no-JavaScript flow.

Verify: Following every GET link never changes consent; deliberate valid POST does.

Provisioning then needs a coherent SCIM contract.

F40 · P1 — SCIM create bodies require server-generated IDs.
File: api-server/src/routes/scim.rs:47, also 73, 288.

Shared input/output DTOs require id and other fields that ordinary create requests omit.

Fix: Separate create/replace DTOs from responses. Generate IDs server-side, define correct optional/default attributes, validate schemas and required names, and return canonical resources with 201 and Location.

Verify: Standard User/Group requests without IDs succeed; omitted optional members and attributes behave correctly. SCIM defines id as server-assigned and read-only. RFC 7643

F41 · P1 — SCIM ignores active=false during user creation.
File: api-server/src/routes/scim.rs:313.

Fix: Persist the requested enabled/disabled lifecycle and return the stored state. Apply identical mapping in create, replace, and patch. Deactivation must revoke sessions and invalidate authentication caches.

Verify: Provision an inactive user, attempt every supported login path, activate through SCIM, then deactivate with an existing session.

F42 · P1 — SCIM filter lookup and User PATCH are missing.
File: api-server/src/routes/scim.rs:90, also router declarations and 240.

deny_unknown_fields rejects filter, and Users lack the PATCH support present for Groups.

Fix: Implement bounded, parsed, parameterized filters for supported attributes and atomic User PATCH operations. Accurately advertise capabilities and return SCIM-formatted errors.

Verify: Run IdP lookup → create → rename → disable → reactivate → delete, including escaped filters and rollback of invalid multi-operation patches.

F43 · P2 — SCIM pagination metadata contradicts the query, and offset arithmetic can overflow.
File: api-server/src/routes/scim.rs:242, also 280.

Fix: Normalize before subtraction:

let start_index = params.cursor.unwrap_or(params.start_index).max(1);
let offset = start_index - 1;
let count = params.count.clamp(0, MAX_SCIM_COUNT);

Return the normalized start and actual resource count. count=0 must return no resources and the total. Negative counts normalize to zero; start indexes below one normalize to one. RFC 7644 pagination

Verify: Include i64::MIN, negative/zero counts, capped counts, final pages, and the cursor alias.

Next align the SDKs and published examples with actual delivered behavior.

F47 · P1 — A clean Python SDK installation lacks an import dependency.
Files: packages/sdk-python/pyproject.toml:37; src/apexmail/models.py:64.

EmailStr requires email-validator, which is not declared. Initial test collection failed; manually adding it allowed 58 tests to pass.

Fix: Declare pydantic[email]>=2.0.0 with the intended version bounds, or explicitly declare compatible email-validator as a runtime dependency.

Verify: Build the wheel and install it into an empty supported Python environment using only its declared dependencies.

F48 · P1 — SDKs silently discard accepted email options.

File	Accepted inputs omitted from delivery requests
sdk-python/.../resources/emails.py:109,238	Reply-To, attachments, custom headers; address coercion drops display names.
sdk-go/apexmail.go:865	Reply-To, template ID/data, attachments, priority, display names.
sdk-java/.../Emails.java:58	Reply-To, template ID, attachments, priority, display names.
sdk-ruby/lib/apexmail.rb:406	Reply-To, template ID/data, attachments, priority, display names.
sdk-php/src/Resources/Emails.php:139	Reply-To, template ID/data, attachments, priority, display names.

Fix: Implement these advertised capabilities through api-server/src/routes/messages.rs:64, persistence, queue models, PreparedEmail, and both transports. Validate decoded attachment sizes, MIME metadata, reserved headers, and template ownership/version. Update OpenAPI and every serializer together. During implementation, explicitly reject unsupported non-default options before sending.

Verify: Capture SDK request bodies and delivered MIME. Every supplied option must have its intended effect.

F51 · P2 — Features-page examples use incorrect response/tag shapes.
File: apps/marketing-zola/templates/partials/features/details.html:57, also 61, 113, 119.

The recent template repair did not fix these earlier examples.

Fix: For message endpoints, check status and read response.json()["data"], then its ID/status. Send tags as a string list. Share executable contract fixtures across docs, marketing, and SDKs.

Verify: Execute each example against the canonical API, including its follow-up status request.

F52 · P1 — Required UI/HTML gates can silently pass without running.
File: ci/stages/test.sh:223, also 234.

Missing Node/Playwright causes successful early return before the independent Python tag-balance check.

Fix: Run tag-balance unconditionally after building fixtures. Provision required browser dependencies or fail the required gate explicitly. Distinguish intentionally disabled optional checks from missing infrastructure.

Verify: An unmatched tag fails even without Node modules; a required browser gate cannot report success without executing.

F53 · P3 — README gives an invalid Zola invocation.
File: README.md:64, also 272.

Fix: Replace both instances with:

zola --root apps/marketing-zola build

The pinned Zola rejects placing --root after build.

Verify: Copy the documented command from repository root using Zola 0.22.1.

The compliance service has additional routing and data-contract failures.

F56 · P1 — Thirty-two routes use capture syntax incompatible with pinned Axum.
Files: compliance/src/routes.rs:103; compliance/src/admin_routes.rs:36; dependency compliance/Cargo.toml:36.

Axum is 0.7.9, but 16 route declarations in each file use {name} captures.

Fix: Within .route(...) URL literals, change each {name} to :name, including both captures in multi-parameter routes. Leave documentation placeholders and Rust formatting braces untouched. A dependency upgrade would require migrating the connected router stack consistently.

Verify: Dispatch concrete values through all 32 declarations and verify handler selection, authorization, and extracted parameters.

F57 · P1 — Risk assessment reads three nonexistent tables.
File: compliance/src/risk_scoring.rs:262, also 268, 285.

spam_complaints, unsubscribes, and content_violations are absent. Their try_join! failure aborts assessment.

Fix: Read canonical complaint/unsubscribe events through one tenant-scoped metrics adapter with explicit event mapping/windows. If separate violation history is required, provide its migration, producer, and retention together. Required source failure must remain visible.

Verify: Seed nonzero factors and check the resulting assessment; missing required storage must fail readiness/assessment explicitly.

F59 · P2 — Queue metrics retain stale nonzero values after draining.
File: worker-processors/src/email/processor.rs:1174.

Only returned GROUP BY statuses are emitted; absent groups retain their old gauge values.

Fix: Initialize every supported status to zero for each successful snapshot, overlay query results, and emit the complete bounded set. Expose freshness/error separately when the query fails.

Verify: Drain pending/processing rows and confirm both gauges reach zero without reporting false zeroes during database outages.

Finally, these components contain concrete defects but are dormant, optional, or not fully connected. Their status matters: they should be implemented as complete features before being relied on.

F60 · P2 — Credit-note persistence and replay semantics are incomplete.
File: billing-service/src/credit_notes.rs:120, also 157, 176, 290.

credit_notes is absent; no production caller was found. Remaining-credit validation precedes replay lookup, so an identical full-credit retry can fail. Key lookup is global.

Fix: Add canonical persistence with invoice/tenant ownership, positive amount, currency, audit data, and unique (tenant_id,idempotency_key). Resolve and compare replays before reserving new credit. Commit invoice lock, credit, wallet allocation, and audit together.

Verify: Full-credit replay returns the original result; concurrent credits cannot exceed the eligible balance.

F61 · P2 — The standalone entitlement helper joins absent tables.
File: compliance/src/entitlements.rs:76, also 81.

subscription_contracts and plan_discounts are absent; no production caller was found.

Fix: Connect this helper to the authoritative entitlement model, incorporating effective plans, valid overrides, contracts, and discounts. Any new persistence requires producers, validity/overlap constraints, and typed tenant relationships—not empty tables that conceal missing behavior.

Verify: API, console, send quota, and this response agree for trial, paid, overridden, suspended, and expired tenants.

F62 · P2 — The alternative template repository uses the wrong table and tenant type.
File: template-renderer/src/renderer.rs:172, also 196; routes.rs:121.

Database helpers use absent email_templates and UUID tenants; canonical templates use text tenant IDs. The source-rendering HTTP path is separate.

Fix: Adapt stored rendering to the canonical template/version repository, shared ownership checks, and cache invalidation. Preserve source-only rendering as its explicit contract.

Verify: Create and version a template through the API, render current/historical versions, and reject foreign-tenant access.

F63 · P2 — Optional components lack migrations for their persistence contracts.

File beneath services/mail-server/crates/	Missing relation(s)
edge-cases/src/services/attachment.rs:303	edge_attachment_scans
edge-cases/src/services/calendar.rs:225	edge_calendar_events
edge-cases/src/services/delivery.rs:486,511,550	edge_delivery_attempts
edge-cases/src/services/eai.rs:254,400	edge_domain_capabilities
ha/src/backup.rs:246,1044	ha_backups
ha/src/chaos.rs:121,389	ha_chaos_experiments
ha/src/multi_region.rs:222,310,339,370,403	ha_regions, ha_geo_routing_rules
isolation/src/audit.rs:250,319,402	iso_audit_logs
isolation/src/data_isolation.rs:363,550,566,596	iso_access_policies, iso_workspaces
isolation/src/encryption.rs:296,556,567	iso_encryption_keys
isolation/src/tenant.rs:206,336,548 and subsequent CRUD methods	iso_organizations, iso_workspaces, iso_workspace_members
compliance/src/estonia_ou.rs:2064	contact_persons

Fix: For each enabled component, add numbered canonical migrations matching its actual INSERT/SELECT/UPDATE contracts and row types. Define canonical tenant/workspace foreign keys, uniqueness, indexes, retention, and deletion behavior. Connect writers, readers, and lifecycle jobs together; verify required schema at readiness.

Verify: Fresh migration → create/read/update/lifecycle → restart → reread, with tenant isolation. These are conditional component failures, not a claim that all are currently deployed.

F67 · P2 — Separate reply analytics has no canonical persistence.
File: analytics/src/reply_tracking.rs:107, also 114, 150, 184, 206.

reply_events is absent; no initializer or production caller was found.

Fix: Add its canonical model and durable ingress adapter: tenant/message identity, idempotent event identity, recipient, subject, automatic-reply flag, sentiment, integer thread depth, and timestamp. Index tenant/time and tenant/message. With integer depth, select AVG(thread_depth)::double precision for the existing f64 result.

Verify: Ingest/retry/restart without duplicate counts; stored reply metrics survive.

F68 · P2 — Reply metrics cache ignores the requested time window.
File: analytics/src/reply_tracking.rs:142, also 94, 143, 200.

get_metrics(tenant_id, days) caches only by tenant, so one-day requests can receive cached 30-day results.

Fix: Include the validated window/filter identity in the key and invalidate all affected windows, or use a tenant data-generation number. Bound days before constructing the duration.

Verify: Alternate one-, seven-, and thirty-day queries against warm caches; each must match its own underlying window.

F69 · P2 — Reply thread lookup omits tenant ownership.
File: analytics/src/reply_tracking.rs:110, also 204, 206.

Thread depth is selected by external message ID alone. A foreign tenant’s matching ID can influence the result once this component is connected.

Fix: Pass event.tenant_id into get_thread_depth, add the tenant predicate, use tenant-qualified message identity, and bound depth arithmetic.

Verify: Give two tenants the same external Message-ID with different depths. Each reply must derive its depth exclusively from its own tenant.