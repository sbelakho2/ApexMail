[package]
name = "apexmail-canonical-config"
version = "1.0.0"
edition = "2021"
description = "Single source of truth for ApexMail company, product, pricing, and claims data"
license = "AGPL-3.0-or-later"

[lib]
name = "apexmail_canonical_config"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
