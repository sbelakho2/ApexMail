[package]
name = "migrator"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "ApexMail one-shot database migration runner (sqlx::migrate! over the workspace migrations)"

[[bin]]
name = "migrator"
path = "src/main.rs"

[dependencies]
# Async runtime
tokio.workspace = true

# Database (migrations)
sqlx.workspace = true

# Error handling / logging
anyhow.workspace = true
tracing-subscriber.workspace = true
