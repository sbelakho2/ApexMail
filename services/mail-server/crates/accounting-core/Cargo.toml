[package]
name = "accounting-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "ApexMail statutory accounting core — journals, periods, source documents, derivation"

[dependencies]
sqlx = { workspace = true }
# Bank statement CSV parsing (the format the product receives; see
# src/bank_ingest.rs). The crate is already in the workspace lockfile via
# api-server.
csv = "1.3"
chrono = { workspace = true }
uuid = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["test-util", "macros", "rt-multi-thread"] }
migrator = { path = "../migrator" }
