-- Reserved SQLx migration slot.
-- Historical schema work for this version lived in the legacy tools/migrations
-- initdb path before services/mail-server/migrations became the source of truth.
SELECT 1;