# Fail hung tests instead of stalling the whole run: a test exceeding the
# slow period is retried once, then terminated. Retries absorb one-off
# scheduling stalls; a genuine hang fails the run with a thread dump in the
# JUNIT output rather than blocking the pipeline for hours.
[profile.default]
slow-timeout = { period = "300s", terminate-after = 2 }
