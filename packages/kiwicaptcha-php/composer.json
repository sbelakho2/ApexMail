{
    "name": "kiwicaptcha/kiwicaptcha-php",
    "description": "KiwiCaptcha: privacy-first, self-hosted proof-of-work anti-abuse engine for PHP. Zero third-party services or tracking; optional first-party telemetry; pure-PHP verification.",
    "type": "library",
    "license": "MIT",
    "keywords": ["captcha", "proof-of-work", "sha256", "argon2", "security", "privacy"],
    "require": {
        "php": ">=8.1",
        "ext-json": "*",
        "ext-openssl": "*",
        "ext-sodium": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5",
        "predis/predis": "^3.5",
        "psr/cache": "^3.0",
        "psr/log": "^3.0"
    },
    "suggest": {
        "psr/cache": "Required for the PSR-6 challenge storage adapter (Psr6Storage)",
        "psr/log": "Optional structured logging of verify outcomes",
        "bel-consulting/kiwicaptcha-symfony": "Symfony bundle integration (form type, validator constraint, Twig widget, challenge endpoint)"
    },
    "autoload": {
        "psr-4": {
            "KiwiCaptcha\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "KiwiCaptcha\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "extra": {
        "branch-alias": {
            "dev-main": "1.0.x-dev"
        }
    },
    "minimum-stability": "stable"
}
