[package]
name = "kiwicaptcha-embed-worker"
version = "0.1.0"
edition = "2021"
description = "Generates the KIWI_WORKER_SRC embedded literal in widget-driver.js from assets/kiwi-worker.js (audit rounds 15-16)"
license = "MIT"

[dependencies]
