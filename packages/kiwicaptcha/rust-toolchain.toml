[toolchain]
# Release reproducibility: the Rust compiler is pinned for
# every build — a moving "stable" can change codegen and release bytes.
# Bump deliberately with each release.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
