# ApexMail workspace line-coverage baseline (ratchet) — read by
# ci/stages/test.sh run_coverage_gate after `cargo llvm-cov nextest
# --workspace` (lcov artifact lands in the run dir). BELOW this number =
# stage FAILURE; at-or-above = PASS and the measured value is printed.
#
# The file holds ONE float (percent). Comments (#) and blank lines are
# ignored; the first non-comment line is the value.
#
# Seeded 2026-09-11 from the first instrumented run on the deploy host
# (run 20260911T040144, pipeline 20260911T032656's test stage): 63.0456%
# workspace line coverage. Floored by 0.01 to absorb measurement jitter.
# From here the gate is a pure ratchet: raise it in the SAME PR as the tests
# that earned the increase — that is a PR-positive action; lowering it
# requires a deliberate, reviewed justification.
63.04
