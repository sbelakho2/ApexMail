global:
  resolve_timeout: 5m

route:
  receiver: default-notify
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: default-notify
    webhook_configs:
      - url: 'http://127.0.0.1:5001/alerts'
        send_resolved: true
