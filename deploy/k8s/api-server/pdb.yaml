apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api-server
  namespace: apexmail
  labels:
    app.kubernetes.io/name: api-server
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: api-server