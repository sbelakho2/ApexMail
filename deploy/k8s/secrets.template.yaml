# =============================================================================
# ApexMail Secrets — TEMPLATE
#
# ⚠️  DO NOT commit real values. This file is a template; real values are
#      managed via Kubernetes External Secrets Operator (ESO), Vault, or
#      sealed-secrets.
#
# To create a test secret manually:
#   kubectl create secret generic apexmail-secrets \
#     --from-literal=tracking-secret-key='...' \
#     --from-literal=db-password='...' \
#     --from-literal=database-url='postgresql://...' \
#     --from-literal=redis-password='...' \
#     --from-literal=redis-url='redis://:...@redis:6379/0' \
#     --from-literal=clickhouse-password='...' \
#     --from-file=jwt-private-key-pem=./jwt-private.pem \
#     --from-file=jwt-public-key-pem=./jwt-public.pem \
#     --from-literal=api-key-hash-secret='...' \
#     --from-literal=webhook-signing-secret='...' \
#     --from-literal=session-secret='...' \
#     --from-literal=impersonation-secret='...' \
#     --from-literal=csrf-secret='...' \
#     -n apexmail
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: apexmail-secrets
  namespace: apexmail
  labels:
    app.kubernetes.io/name: apexmail
  annotations:
    # Hint ESO / Vault agent to manage this Secret
    secrets.apexmail.ee/managed-by: "external-secrets"
type: Opaque
stringData:
  tracking-secret-key: "REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS"
  db-password: "REPLACE_ME"
  database-url: "postgresql://apexmail:REPLACE_ME@postgres.apexmail.svc.cluster.local:5432/apexmail"
  redis-password: "REPLACE_ME"
  redis-url: "redis://:REPLACE_ME@redis.apexmail.svc.cluster.local:6379/0"
  clickhouse-password: "REPLACE_ME"
  jwt-private-key-pem: |-
    -----BEGIN PRIVATE KEY-----
    REPLACE_ME
    -----END PRIVATE KEY-----
  jwt-public-key-pem: |-
    -----BEGIN PUBLIC KEY-----
    REPLACE_ME
    -----END PUBLIC KEY-----
  api-key-hash-secret: "REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS"
  webhook-signing-secret: "REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS"
  session-secret: "REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS"
  impersonation-secret: "REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS"
  csrf-secret: "REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS"
  billing-company-iban: "REPLACE_ME"
  billing-company-phone: "REPLACE_ME"
