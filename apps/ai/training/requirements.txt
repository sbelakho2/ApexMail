# =============================================================================
#  ApexMail AI — Qwen 2.5-7B-Instruct Fine-Tuning on vast.ai
# =============================================================================
#
#  Core stack:
#    - PyTorch 2.3+  (CUDA 12.x)
#    - transformers + peft + trl  (QLoRA SFT)
#    - bitsandbytes              (4-bit NF4 quantisation)
#    - flash-attn 2              (memory-efficient attention)
#    - optimum + onnxruntime     (ONNX export for prod)
#
#  Designed for a single-GPU vast.ai instance (A100 40 GB / A6000 48 GB).
# =============================================================================

# --- PyTorch (CUDA 12.x wheels auto-selected by pip when CUDA toolkit present)
torch>=2.3.0
torchvision>=0.18.0

# --- Hugging Face ecosystem
transformers>=4.44.0
peft>=0.12.0
trl>=0.10.0
accelerate>=0.33.0
datasets>=2.20.0

# --- Quantisation
bitsandbytes>=0.43.0

# --- Flash Attention 2 (requires CUDA ≥ 11.6, Ampere+)
flash-attn>=2.6.0

# --- ONNX export & runtime
optimum[onnxruntime-gpu]>=1.21.0
onnx>=1.16.0
onnxruntime-gpu>=1.18.0

# --- Tokeniser backends
sentencepiece>=0.2.0
protobuf>=5.27.0

# --- Evaluation & metrics
evaluate>=0.4.2
rouge-score>=0.1.2
nltk>=3.9.0
scikit-learn>=1.5.0
scipy>=1.14.0

# --- Logging & monitoring
tensorboard>=2.17.0
wandb>=0.17.0

# --- Utilities
safetensors>=0.4.3
huggingface-hub>=0.24.0
pyyaml>=6.0.1
rich>=13.7.0
typer>=0.12.0
gguf>=0.9.0
