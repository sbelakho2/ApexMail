# Compiled native addons (napi-rs / native build outputs)
*.node

# ─── Build & cache outputs ────────────────────────────────────────────────────
out/
**/out/
dist/
**/dist/
build/
**/build/
.turbo/
**/.turbo/
.cache/
**/.cache/
.eslintcache
*.tsbuildinfo

# ─── Rust build artefacts ─────────────────────────────────────────────────────
target/
**/target/

# ─── Python ───────────────────────────────────────────────────────────────────
.venv/
venv/
.env_python/
__pycache__/
**/__pycache__/
apps/marketing-zola/tailwindcss-*
apps/marketing-zola/public/
*.pyc
*.pyo
*.pyd
.pytest_cache/
**/.pytest_cache/
.mypy_cache/
**/.mypy_cache/
.ruff_cache/
**/.ruff_cache/
*.egg-info/
dist-info/
.ipynb_checkpoints/

# ─── Test artifacts ───────────────────────────────────────────────────────────
coverage/
**/coverage/
.nyc_output/
playwright-report/
**/playwright-report/
test-results/
**/test-results/

# ─── Logs ─────────────────────────────────────────────────────────────────────
*.log
lerna-debug.log*
# Stray root-level test capture files (audit P — were tracked/regenerated junk)
test_output.txt
test_results.log
dump.rdb

# ─── Env / secrets ────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
!.env.sample
!.env.production.example
.vast_api_key
vast_instance_id.txt
# NOTE (audit P + cleanup): the dead deploy/nginx/ssl/dhparam.pem placeholder
# and its generator were REMOVED — nginx.conf never referenced ssl_dhparam
# and its cipher list is pure ECDHE (see deploy/nginx/ssl/README.md).
# All .pem files (real cert material) are excluded below.
*.pem
*.key
secrets/
**/secrets/
*.p12
*.pfx
*.jks

# ─── OS / Editor ──────────────────────────────────────────────────────────────
.DS_Store
**/.DS_Store
*.swp
*.swo
*.bak
.idea/
.vscode/
*.orig

# ─── Retired AI / ML artefacts (never a deployable product surface) ──────────
apps/ai/training/onnx_model/
apps/ai/training/output/
apps/ai/training/output_agent/
apps/ai/training/output-merged/
apps/ai/training/output_*/
apps/ai/training/merged_*/
apps/ai/training/gguf_model/
apps/ai/training/checkpoints/
apps/ai/training/wandb/
apps/ai/training/runs/
apps/ai/training/eval_results/
apps/ai/training/eval_results_full/
apps/ai/training/artifacts_*/
*.onnx
*.onnx_data
*.safetensors
*.gguf
*.pt
*.pth

# ─── Retired model-training data (generated / large) ─────────────────────────
data/train_agent.jsonl
apps/ai/training/data/train.jsonl
apps/ai/training/data/train.jsonl.gz
apps/ai/training/data/val.jsonl
apps/ai/training/data/test.jsonl
apps/ai/training/data/eval_1000.jsonl
apps/ai/training/data/golden_qa.jsonl

# ─── Local dev scratch files ──────────────────────────────────────────────────
# Tailwind color-palette snippet exports (local dev helpers, not source)
cp_colors.txt
marketing_colors.txt
web_colors.txt
.tmp/
cargo_log.txt
cargo_output.txt

# ─── Misc ─────────────────────────────────────────────────────────────────────
.vercel/
.zcode/

# ─── deploy/tests validator report artifacts (F14: captured into ci/runs/<ts>/) ──
deploy/tests/.contrast-check-report.json
deploy/tests/.html-validation-report.json
deploy/tests/.seo-validation-report.json
deploy/tests/.broken-links-report.json
deploy/tests/.font-optimization-report.json
deploy/tests/.image-optimization-report.json
deploy/tests/.performance-budget-report.json
deploy/tests/.cta-tracking-report.json
# the broken-links crawl list (rewritten on every run; a tracked copy made
# the deploy checkout permanently dirty and would break git pull --ff-only)
deploy/tests/.routes.txt

# content voice-check report artifact (generated)
deploy/tests/.content-voice-report.json
deploy/tests/.signup-test-report.json

# AI training artifacts — never commit model weights or environments
apps/ai/training/.venv/
apps/ai/training/output_*/
apps/ai/training/merged_*/
apps/ai/training/__pycache__/
apps/ai/training/*.safetensors
apps/ai/training/**/*.pt

# Build/report artifacts and training data — never tracked (audit 2026-09-05)
/reports/
/data/
/test_output.txt
tools/contrast-audit/node_modules/
dump.rdb
