{
  "schemaVersion": 2,
  "updatedAt": "2026-08-19",
  "status": "offline-runner",
  "purpose": "Records the reviewed offline model-training runner: corpus and training artifacts are operator-gated, never served by ai-service, and never deployed into the mail path.",
  "datasets": {},
  "policy": {
    "productionUse": false,
    "modelTrainingApproved": true,
    "modelPromotionSupported": false,
    "reason": "ai-service exposes deterministic email-assistance helpers only; training runs execute exclusively through the reviewed offline runner (AI_TRAINING_RUNNER) and require artifact validation before promotion."
  }
}
