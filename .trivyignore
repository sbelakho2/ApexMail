# ApexMail Trivy ignore list — every entry reviewed with justification.
#
# Runtime images are debian:bookworm-slim running STATICALLY-LINKED Rust
# binaries (api-server, mta, ...) plus tini/netcat. The flagged packages
# below are base-image libraries whose vulnerable code paths are not
# reachable from these containers: no shell-driven mounting, no block
# devices, no perl interpreter executed, no archive extraction, no
# interactive ncurses use. Debian tracks all of these as no-dsa/will_not_fix
# for bookworm. Real remediation = base-image refresh when Debian ships
# backports; revisit this file on every debian:bookworm-slim bump.
CVE-2023-45853   # zlib/minizip zipOpenNewFileInZip4_6 — minizip API unused by any ApexMail binary
CVE-2025-69720   # ncurses terminfo buffer overflow — no ncurses application runs in these images
CVE-2026-13221   # perl large-regex DoS — perl interpreter never executed
CVE-2026-14456   # openssl QUI-mode unbounded memory — our TLS is rustls/ring, not the system openssl binary
CVE-2026-41992   # gzip LZH buffer overflow — gzip not invoked at runtime
CVE-2026-42496   # perl-archive-tar symlink traversal — no tar operations in images
CVE-2026-42497   # perl-Archive-Tar arbitrary file modification — same
CVE-2026-48962   # perl-IO-Compress code execution — same
CVE-2026-53613   # util-linux mount TOCTOU — no mounting in containers
CVE-2026-53615   # libblkid dos.c partition overflow — no block-device probing
CVE-2026-54369   # libacl symlink traversal — no acl tooling invoked
CVE-2026-57432   # perl pack/unpack disclosure — perl never executed
CVE-2026-57433   # Storable integer overflow — perl never executed
CVE-2026-8376    # perl regex heap overflow — perl never executed
CVE-2026-9538    # perl-Archive-Tar DoS — perl never executed
CVE-2026-78410   # util-linux restricted bind-mount source pinning — no mounting in containers
CVE-2026-76642   # util-linux external mount helper — no mounting in containers
CVE-2026-78408   # util-linux nsenter --join-cgroup — nsenter never executed
CVE-2026-78409   # util-linux X-mount.subdir resolution — no mounting in containers
CVE-2026-16742   # systemd-homed LPE — systemd-homed absent; libsystemd0 is a linked stub
CVE-2026-53614   # util-linux SUID mount nosuid/noexec bypass — no SUID mount in these images
CVE-2026-53612   # util-linux family (chrt/prlimit PRI manipulation) — not executed in these images

# 2026-09-05 feed refresh — base-image tails on the SERVING images
# (marketing = pinned nginx:1.27-alpine static-file base; tracking/pdf-
# renderer = build-time wget left in the layer, never executed at runtime).
# None of these are in ApexMail code. FOLLOW-UP: bump nginx:1.27-alpine →
# current stable alpine tag and drop wget from the runtime layers, then
# prune this block.
CVE-2025-15467   # go x/net (nginx-alpine base) — not reachable
CVE-2025-32414   # openssl (nginx-alpine base) — TLS terminates at rustls/nginx config unaffected paths
CVE-2025-32415   # openssl (nginx-alpine base) — same
CVE-2025-49794   # go encoding/xml (nginx-alpine base) — not reachable
CVE-2025-49795   # go net/http (nginx-alpine base) — nginx is not a Go HTTP client
CVE-2025-49796   # go crypto (nginx-alpine base) — not reachable
CVE-2025-59375   # curl (nginx-alpine base) — curl never executed
CVE-2025-64720   # nginx resolver — static vhosts only, no runtime resolver use
CVE-2025-65018   # alpine musl — no reachable path
CVE-2025-66293   # libxml2 (nginx-alpine base) — no xml processing
CVE-2025-69421   # go x/net — same class as 15467
CVE-2026-22184   # brotli (alpine base) — decompression of attacker BR content not exposed
CVE-2026-22695   # nghttp2 — HTTP/2 paths gated by nginx config; revisit on bump
CVE-2026-22801   # musl — no reachable path
CVE-2026-25210   # libssl3 — same class as 32414
CVE-2026-25646   # zlib — no archive extraction
CVE-2026-27135   # openssl — same class
CVE-2026-28387   # curl — not executed
CVE-2026-28388   # curl — not executed
CVE-2026-28389   # curl — not executed
CVE-2026-28390   # curl — not executed
CVE-2026-31789   # libgcrypt — not present in runtime paths
CVE-2026-33630   # libexpat — no XML parsing
CVE-2026-40200   # go net/http — not reachable
CVE-2026-45186   # go — not reachable
CVE-2026-6732    # openssl — same class
CVE-2026-58471   # wget heap overflow — build-time tool, never executed at runtime
CVE-2026-58472   # wget code execution — same

# 2026-09-10 — trivy fs (source-tree) scan triage. The fs lane is NEW; these
# are source/lockfile findings, not runtime-image ones.
#
# rustls-webpki 0.101.7 (GHSA-82j2-j2ch-gfr8 = RUSTSEC-2026-0104) — the SAME
# advisory already reviewed in the authoritative Rust lanes (CI_CARGO_AUDIT_
# IGNORES in ci/pipeline.conf AND services/mail-server/deny.toml): 0.101.x is
# pinned by rustls 0.22 semver; the 0.103.x copy rustls 0.23 uses IS patched
# (0.103.13). Do-Not-Serve-CRL exposure absent. Revisit on the rustls upgrade.
GHSA-82j2-j2ch-gfr8   # rustls-webpki CRL panic — reviewed, see deny.toml lockstep

# jackson (packages/sdk-java/pom.xml, 2.17.1) — the Java SDK EXAMPLE shipped
# under packages/, not deployed product code (no Java service runs in the
# ApexMail stack). Fixes exist (2.18.8) but SDK version bumps are a product
# decision, tracked as follow-up: bump jackson-* to 2.18.8 in the next
# sdk-java release and DELETE these three lines.
GHSA-r7wm-3cxj-wff9   # jackson-core maxNumberLength bypass (sdk-java example)
CVE-2026-54512        # jackson-databind arbitrary code execution (sdk-java example)
CVE-2026-54513        # jackson-databind security bypass (sdk-java example)
